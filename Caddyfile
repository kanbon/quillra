# Example: TLS terminate at Caddy, proxy to Quillra Node server.
# Replace cms.example.com with your domain.

cms.example.com {
	encode gzip
	reverse_proxy 127.0.0.1:3000 {
		header_up X-Forwarded-For {remote_host}
		header_up -CF-Connecting-IP
		header_up -X-Real-IP
	}
}
